    case_name: collect_addtool_01
    host: https://www.wanandroid.com
    url: /lg/collect/addtool/json
    method: POST
    detail: 新增收藏网址接口
    headers:
      # 这里cookie的值，写的是存入缓存的名称
      cookie: $cache{login_cookie}
    # 请求的数据，是 params 还是 json、或者file、data
    requestType: data
    data:
      name: biying
      link: https://cn.bing.com

    dependence_case: False
    # 依赖的数据
    dependence_case_data:
    assert:
      # 断言接口状态码
      errorCode:
        jsonpath: $.errorCode
        type: code
        value: 0
        AssertType:

    current_request_set_cache:
      - type: response
        jsonpath: $.data.id
        # 自定义的缓存名称
        name: collect_addtool_01
    sql:
    teardown:
      - case_id: collect_delete_tool_01
        send_request:
          - dependent_type: response
            jsonpath: $.data.id
            replace_key: $.data.id

    teardown_sql:
      - UPDATE `api_test`.`ysq_test` SET `name` = '$json($.data.id)$' WHERE `name` = '2' LIMIT 1


